node_modules/
dist/
*.log
*.mcpb
.DS_Store
.claude/settings.local.json
.impeccable/

# Agent tooling cache (never ship)
.agent-transcripts/
.cursor/projects/

# Chakra UI agent skills (installed via `npx skills add`)
.agents/
skills-lock.json
.claude/skills/chakra-ui-*

# generated community-UI staging (rebuild() syncs from the user plugin dir)
plugins-community/*
!plugins-community/README.md

# Local assistant instructions, not part of the published tool.
CLAUDE.md
